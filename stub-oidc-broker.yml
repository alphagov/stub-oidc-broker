server:
  applicationConnectors:
    - type: http
      port: ${APPLICATION_PORT:-6610}
  adminConnectors:
    - type: http
      port: ${ADMIN_PORT:-6611}

stubBrokerURI: ${STUB_BROKER_URI:-"http://localhost:6610"}

stubTrustframeworkRP: ${STUB_TRUSTFRAMEWORK_RP:-"http://localhost:4410"}

redisURI: ${REDIS_URI:-"redis://192.168.204.170:6380"}

redisDatabase: ${REDIS_DATABASE:-"/1"}

directoryURI: ${DIRECTORY_URI:-"http://localhost:3000"}

middlewareURI: ${MIDDLEWARE_URI:-"http://localhost:3001"}

verifiableCredentialURI: ${VERIFIABLE_CREDENTIAL_URI:-"http://localhost:3333"}

local: ${IS_LOCAL:-true}

softwareID: "Stub OIDC Broker Software"

scheme: ${SCHEME:-"1"}

branding: ${BRANDING:-"private"}
